/**
 * Bootstrap — public entry point.
 *
 * The implementation lives in ./services/Bootstrap.js next
 * to the services it wires up; this shim keeps the import
 * path documented in the README (`core/Bootstrap.js`) stable.
 */

export { Bootstrap } from "./services/Bootstrap.js";
